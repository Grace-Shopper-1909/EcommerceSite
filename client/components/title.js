import React from 'react'
import {Link} from 'react-router-dom'
import AllProducts from './all-products'

const Title = props => {
  return <h1 id="rainbow">{props.title}</h1>
}

export default Title
