const {db, User, Product} = require('./models')
// const Order = require('./models/orders')
// const Cart = require('./models/cart')

module.exports = {
  db,
  User,
  Product
  // Order,
  // Cart
}
